# OpenMAIC 架构说明（比赛版）

本文用于快速说明本项目的系统架构，便于开发协作、比赛答辩和技术评审。

## 1. 架构概览

OpenMAIC 采用 Next.js 全栈一体化架构：

- 前端（UI）和后端（API）在同一仓库内。
- 通过 App Router 提供页面路由与 API Route。
- 核心业务能力下沉到 lib 目录，API 层主要负责输入校验、鉴权、编排调用和流式返回。
- 通过环境变量对接外部模型、语音、图像、视频与检索服务。

一句话总结：这是一个“单仓全栈 + 内置 BFF + 多模型编排”的 AI 教学应用。

## 2. 分层设计

### 2.1 表现层（Frontend）

主要职责：

- 提供课堂创建、内容预览、互动学习、设置配置等用户界面。
- 管理本地 UI 状态（主题、语言、草稿、最近记录等）。
- 发起对 API 的请求并消费流式响应。

关键目录：

- app/page.tsx：首页与课堂创建入口。
- app/classroom：课堂播放与互动页面。
- components：通用 UI 组件、业务组件。

### 2.2 接口层（API / BFF）

主要职责：

- 对外提供统一 API（聊天、课堂生成、媒体生成、解析与校验）。
- 做请求参数校验与错误规范化。
- 把前端请求转换为业务层调用，并以 SSE/JSON 形式返回。

关键目录：

- app/api/chat/route.ts：流式聊天接口（SSE）。
- app/api/generate-classroom/route.ts：课堂生成任务接口。
- app/api/generate/*：图片、视频、TTS、大纲等生成能力。
- app/api/parse-pdf/route.ts：PDF 解析。
- app/api/verify-*/route.ts：模型和服务可用性校验。

### 2.3 鉴权与请求保护层

主要职责：

- 对页面/API 请求进行访问控制。
- 支持 ACCESS_CODE 场景下的 Cookie 校验和 API 防护。

关键文件：

- middleware.ts

### 2.4 领域与编排层（Domain / Orchestration）

主要职责：

- 多智能体流程编排。
- 教学场景生成、对话状态推进、工具调用与结果拼装。
- 对接模型提供商与统一能力抽象。

关键目录：

- lib/orchestration：生成与会话编排核心。
- lib/generation：课堂内容生成相关逻辑。
- lib/ai：模型提供商与调用适配。
- lib/server：服务端通用逻辑（解析、模型解析、响应工具等）。

### 2.5 数据与存储层

主要职责：

- 浏览器侧持久化（课堂、配置、缓存等）。
- 媒体文件和导入导出数据管理。

关键目录：

- lib/storage
- lib/store
- lib/import
- lib/export

### 2.6 内部包层（Workspace Packages）

主要职责：

- 抽离通用或历史兼容能力，作为 workspace 包复用。

关键目录：

- packages/mathml2omml
- packages/pptxgenjs

## 3. 典型请求链路

### 3.1 课堂生成链路

1. 用户在前端填写需求并上传资料。
2. 前端调用 app/api/generate-classroom 或 app/api/generate/*。
3. API 层进行参数校验和模型配置解析。
4. 编排层执行多智能体生成流程。
5. 结果回传前端（部分接口为流式返回）。
6. 前端渲染课堂并写入本地存储。

### 3.2 聊天链路（SSE）

1. 前端调用 app/api/chat。
2. API 层读取完整会话状态并进入 stateless 生成流程。
3. 服务端按事件流（SSE）持续推送增量消息。
4. 前端边接收边渲染，支持中断与恢复。

## 4. 前后端关系说明

本项目是“前后端分层、同仓部署”：

- 有明确前端层（React 页面与组件）。
- 有明确后端层（Next API Routes）。
- 但不是前后端两个独立仓库，也不是前后端两套部署系统。

这种架构对比赛有三个直接优势：

- 开发与调试链路短，迭代快。
- 部署简单，环境一致性更好。
- 演示时故障面更小，稳定性更可控。

## 5. 关键外部依赖能力

- LLM 提供商（对话与生成）
- TTS / ASR（语音相关）
- 图像/视频生成服务
- PDF 解析服务
- Web Search 服务

这些能力通过环境变量配置接入，便于在不同比赛环境中替换。

## 6. 非功能设计要点

- 可维护性：通过 lib 分层减少页面与业务逻辑耦合。
- 可扩展性：provider 抽象便于新增模型和服务商。
- 可观测性：服务端有日志与错误规范化返回。
- 安全性：middleware 对 API 访问进行基础保护（可选 ACCESS_CODE）。

## 7. 比赛改造建议（架构层）

1. 保持 API 契约稳定，优先在 lib 层扩展能力，减少页面层侵入式改动。
2. 关键链路（课堂生成、聊天、导出）增加降级策略与兜底提示。
3. 对外部服务调用增加超时和重试策略，避免现场演示卡死。
4. 为核心能力准备离线或弱网替代路径（至少保证演示主流程可走通）。

## 8. 建议答辩讲法（3 分钟版本）

1. 先讲分层：前端交互层、API 接口层、多智能体编排层、外部模型层。
2. 再讲链路：从“用户输入”到“流式课堂输出”的完整路径。
3. 最后讲价值：为什么同仓全栈架构更适合比赛快速迭代与稳定演示。
